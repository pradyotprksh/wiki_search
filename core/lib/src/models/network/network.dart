/// This contains all the models which will be used while making a network call.
/// i.e. the network body which will be send in the network call or the
/// response of the network call after successful call.
export 'wiki_search_response.dart';
