/// Contains all the binding class which each route and the child routes will
/// used to update the UI based on the controller only. It contains only the
/// UI based controller code like page controller, nodes, scroll listener, etc.
/// Will be used to remove the view related code from the ui module. This will
/// make UI module code much more cleaner.
export 'initial_bindings.dart';
