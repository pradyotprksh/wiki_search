/// A shared module which will contain all the shared code used by both
/// core and the view module. This will keep all the constants and widgets
/// into one place and not make the view module file structure big.

library shared;

export 'src/utils/utils.dart';
